plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
    id 'org.jetbrains.kotlin.kapt'
}

def releaseKeystorePath =
    System.getenv("SHIGUANGBOX_KEYSTORE_FILE")
def releaseStorePassword =
    System.getenv("SHIGUANGBOX_STORE_PASSWORD")
def releaseKeyAlias =
    System.getenv("SHIGUANGBOX_KEY_ALIAS")
def releaseKeyPassword =
    System.getenv("SHIGUANGBOX_KEY_PASSWORD")

def releaseSigningReady =
    [
        releaseKeystorePath,
        releaseStorePassword,
        releaseKeyAlias,
        releaseKeyPassword
    ].every {
        it != null &&
            !it.trim().isEmpty()
    }

android {
    namespace 'com.shiguangbox.app'
    compileSdk 35

    defaultConfig {
        applicationId 'com.shiguangbox.app'
        minSdk 26
        targetSdk 35
        versionCode 40
        versionName '1.7.6'
    }

    signingConfigs {
        release {
            if (releaseSigningReady) {
                storeFile = file(releaseKeystorePath)
                storePassword = releaseStorePassword
                keyAlias = releaseKeyAlias
                keyPassword = releaseKeyPassword
            }
        }
    }

    buildTypes {
        release {
            if (releaseSigningReady) {
                signingConfig = signingConfigs.release
            }
            minifyEnabled false
            shrinkResources false
        }
    }

    buildFeatures {
        compose true
    }

    composeOptions {
        kotlinCompilerExtensionVersion '1.5.14'
    }

    kotlinOptions {
        jvmTarget = '17'
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

def petAssetPartsDir = file("$projectDir/src/main/pet_assets/orange_idle")
def petAssetOutput = file("$projectDir/src/main/res/drawable-nodpi/pet_orange_idle.webp")

def decodePetAssets = tasks.register("decodePetAssets") {
    inputs.dir(petAssetPartsDir)
    outputs.file(petAssetOutput)

    doLast {
        def parts = petAssetPartsDir.listFiles()
            ?.findAll { it.name.endsWith(".txt") }
            ?.sort { a, b -> a.name <=> b.name }

        if (parts == null || parts.isEmpty()) {
            throw new GradleException("No orange pet asset chunks found")
        }

        def encoded = parts.collect {
            it.getText("UTF-8").replaceAll("\\s", "")
        }.join("")

        petAssetOutput.parentFile.mkdirs()
        petAssetOutput.bytes = java.util.Base64.decoder.decode(encoded)
        println("Decoded HD pet asset: " + petAssetOutput + " (" + petAssetOutput.length() + " bytes)")
    }
}

def petSleepAssetPartsDir = file("$projectDir/src/main/pet_assets/orange_sleep")
def petSleepAssetOutput = file("$projectDir/src/main/res/drawable-nodpi/pet_orange_sleep.webp")

def decodePetSleepAsset = tasks.register("decodePetSleepAsset") {
    inputs.dir(petSleepAssetPartsDir)
    outputs.file(petSleepAssetOutput)

    doLast {
        def parts = petSleepAssetPartsDir.listFiles()
            ?.findAll { it.name.endsWith(".txt") }
            ?.sort { a, b -> a.name <=> b.name }

        if (parts == null || parts.isEmpty()) {
            throw new GradleException("No orange sleep pet asset chunks found")
        }

        def encoded = parts.collect {
            it.getText("UTF-8").replaceAll("\\s", "")
        }.join("")

        petSleepAssetOutput.parentFile.mkdirs()
        petSleepAssetOutput.bytes = java.util.Base64.decoder.decode(encoded)
        println("Decoded sleep pet asset: " + petSleepAssetOutput + " (" + petSleepAssetOutput.length() + " bytes)")
    }
}

def petTiredAssetPartsDir = file("$projectDir/src/main/pet_assets/orange_tired")
def petTiredAssetOutput = file("$projectDir/src/main/res/drawable-nodpi/pet_orange_tired.webp")

def decodePetTiredAsset = tasks.register("decodePetTiredAsset") {
    inputs.dir(petTiredAssetPartsDir)
    outputs.file(petTiredAssetOutput)

    doLast {
        def parts = petTiredAssetPartsDir.listFiles()
            ?.findAll { it.name.endsWith(".txt") }
            ?.sort { a, b -> a.name <=> b.name }

        if (parts == null || parts.isEmpty()) {
            throw new GradleException("No orange tired pet asset chunks found")
        }

        def encoded = parts.collect {
            it.getText("UTF-8").replaceAll("\\s", "")
        }.join("")

        petTiredAssetOutput.parentFile.mkdirs()
        petTiredAssetOutput.bytes = java.util.Base64.decoder.decode(encoded)
        println("Decoded tired pet asset: " + petTiredAssetOutput + " (" + petTiredAssetOutput.length() + " bytes)")
    }
}

def petWakeAssetPartsDir = file("$projectDir/src/main/pet_assets/orange_wake")
def petWakeAssetOutput = file("$projectDir/src/main/res/drawable-nodpi/pet_orange_wake.webp")

def decodePetWakeAsset = tasks.register("decodePetWakeAsset") {
    inputs.dir(petWakeAssetPartsDir)
    outputs.file(petWakeAssetOutput)

    doLast {
        def parts = petWakeAssetPartsDir.listFiles()
            ?.findAll { it.name.endsWith(".txt") }
            ?.sort { a, b -> a.name <=> b.name }

        if (parts == null || parts.isEmpty()) {
            throw new GradleException("No orange wake pet asset chunks found")
        }

        def encoded = parts.collect {
            it.getText("UTF-8").replaceAll("\\s", "")
        }.join("")

        petWakeAssetOutput.parentFile.mkdirs()
        petWakeAssetOutput.bytes = java.util.Base64.decoder.decode(encoded)
        println("Decoded wake pet asset: " + petWakeAssetOutput + " (" + petWakeAssetOutput.length() + " bytes)")
    }
}

def orangeBubbleAssetPartsDir =
    file("$projectDir/src/main/pet_assets/orange_bubble")

def orangeBubbleAssetOutput =
    file("$projectDir/src/main/res/drawable-nodpi/pet_orange_bubble.webp")

def decodeOrangeBubbleAsset =
    tasks.register("decodeOrangeBubbleAsset") {
        inputs.dir(orangeBubbleAssetPartsDir)
        outputs.file(orangeBubbleAssetOutput)

        doLast {
            def parts =
                orangeBubbleAssetPartsDir
                    .listFiles()
                    ?.findAll {
                        it.name.endsWith(".txt")
                    }
                    ?.sort {
                        a, b ->
                            a.name <=>
                                b.name
                    }

            if (
                parts == null ||
                parts.isEmpty()
            ) {
                throw new GradleException(
                    "No Orange bubble asset chunks found"
                )
            }

            def encoded =
                parts.collect {
                    it.getText("UTF-8")
                        .replaceAll(
                            "\\s",
                            ""
                        )
                }.join("")

            orangeBubbleAssetOutput
                .parentFile
                .mkdirs()

            orangeBubbleAssetOutput.bytes =
                java.util.Base64
                    .decoder
                    .decode(encoded)

            println(
                "Decoded Orange bubble asset: " +
                    orangeBubbleAssetOutput +
                    " (" +
                    orangeBubbleAssetOutput.length() +
                    " bytes)"
            )
        }
    }

def yayaBubbleAssetPartsDir =
    file("$projectDir/src/main/pet_assets/yaya_bubble")

def yayaBubbleAssetOutput =
    file("$projectDir/src/main/res/drawable-nodpi/pet_yaya_bubble.webp")

def decodeYayaBubbleAsset =
    tasks.register("decodeYayaBubbleAsset") {
        inputs.dir(yayaBubbleAssetPartsDir)
        outputs.file(yayaBubbleAssetOutput)

        doLast {
            def parts =
                yayaBubbleAssetPartsDir
                    .listFiles()
                    ?.findAll {
                        it.name.endsWith(".txt")
                    }
                    ?.sort {
                        a, b ->
                            a.name <=>
                                b.name
                    }

            if (
                parts == null ||
                parts.isEmpty()
            ) {
                throw new GradleException(
                    "No Yaya bubble asset chunks found"
                )
            }

            def encoded =
                parts.collect {
                    it.getText("UTF-8")
                        .replaceAll(
                            "\\s",
                            ""
                        )
                }.join("")

            yayaBubbleAssetOutput
                .parentFile
                .mkdirs()

            yayaBubbleAssetOutput.bytes =
                java.util.Base64
                    .decoder
                    .decode(encoded)

            println(
                "Decoded Yaya bubble asset: " +
                    yayaBubbleAssetOutput +
                    " (" +
                    yayaBubbleAssetOutput.length() +
                    " bytes)"
            )
        }
    }

def yutuanRainbowAssetPartsDir =
    file("$projectDir/src/main/pet_assets/yutuan_rainbow")

def yutuanRainbowAssetOutput =
    file("$projectDir/src/main/res/drawable-nodpi/pet_yutuan_rainbow.webp")

def decodeYutuanRainbowAsset =
    tasks.register("decodeYutuanRainbowAsset") {
        inputs.dir(yutuanRainbowAssetPartsDir)
        outputs.file(yutuanRainbowAssetOutput)

        doLast {
            def parts =
                yutuanRainbowAssetPartsDir
                    .listFiles()
                    ?.findAll {
                        it.name.endsWith(".txt")
                    }
                    ?.sort {
                        a, b ->
                            a.name <=>
                                b.name
                    }

            if (
                parts == null ||
                parts.isEmpty()
            ) {
                throw new GradleException(
                    "No Yutuan rainbow asset chunks found"
                )
            }

            def encoded =
                parts.collect {
                    it.getText("UTF-8")
                        .replaceAll(
                            "\\s",
                            ""
                        )
                }.join("")

            yutuanRainbowAssetOutput
                .parentFile
                .mkdirs()

            yutuanRainbowAssetOutput.bytes =
                java.util.Base64
                    .decoder
                    .decode(encoded)

            println(
                "Decoded Yutuan rainbow asset: " +
                    yutuanRainbowAssetOutput +
                    " (" +
                    yutuanRainbowAssetOutput
                        .length() +
                    " bytes)"
            )
        }
    }

def yutuanBubbleAssetPartsDir =
    file("$projectDir/src/main/pet_assets/yutuan_bubble")

def yutuanBubbleAssetOutput =
    file("$projectDir/src/main/res/drawable-nodpi/pet_yutuan_bubble.webp")

def decodeYutuanBubbleAsset =
    tasks.register("decodeYutuanBubbleAsset") {
        inputs.dir(yutuanBubbleAssetPartsDir)
        outputs.file(yutuanBubbleAssetOutput)

        doLast {
            def parts =
                yutuanBubbleAssetPartsDir
                    .listFiles()
                    ?.findAll {
                        it.name.endsWith(".txt")
                    }
                    ?.sort {
                        a, b ->
                            a.name <=>
                                b.name
                    }

            if (
                parts == null ||
                parts.isEmpty()
            ) {
                throw new GradleException(
                    "No Yutuan bubble asset chunks found"
                )
            }

            def encoded =
                parts.collect {
                    it.getText("UTF-8")
                        .replaceAll(
                            "\\s",
                            ""
                        )
                }.join("")

            yutuanBubbleAssetOutput
                .parentFile
                .mkdirs()

            yutuanBubbleAssetOutput.bytes =
                java.util.Base64
                    .decoder
                    .decode(encoded)

            println(
                "Decoded exact Yutuan bubble asset: " +
                    yutuanBubbleAssetOutput +
                    " (" +
                    yutuanBubbleAssetOutput
                        .length() +
                    " bytes)"
            )
        }
    }

tasks.named("preBuild").configure {
    dependsOn(
        decodePetAssets,
        decodePetSleepAsset,
        decodePetTiredAsset,
        decodePetWakeAsset,
        decodeOrangeBubbleAsset,
        decodeYayaBubbleAsset,
        decodeYutuanBubbleAsset,
        decodeYutuanRainbowAsset
    )
}

dependencies {
    implementation platform('androidx.compose:compose-bom:2024.06.00')
    androidTestImplementation platform('androidx.compose:compose-bom:2024.06.00')

    implementation 'androidx.core:core-ktx:1.13.1'
    implementation 'androidx.activity:activity-compose:1.9.1'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.8.4'
    implementation 'androidx.navigation:navigation-compose:2.7.7'
    implementation 'androidx.work:work-runtime-ktx:2.9.1'

    implementation 'androidx.compose.ui:ui'
    implementation 'androidx.compose.ui:ui-tooling-preview'
    implementation 'androidx.compose.foundation:foundation'
    implementation 'androidx.compose.material3:material3'
    implementation 'androidx.compose.material:material-icons-extended'

    implementation 'androidx.room:room-runtime:2.6.1'
    implementation 'androidx.room:room-ktx:2.6.1'
    kapt 'androidx.room:room-compiler:2.6.1'

    debugImplementation 'androidx.compose.ui:ui-tooling'
    debugImplementation 'androidx.compose.ui:ui-test-manifest'
}
